import { createStore } from 'vuex';
import HomeModule from './HomeModule';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    home: HomeModule,
  },
});
